---
import Base from '../../layouts/Base.astro';
import Header from '../../components/v3/Header.astro';
import Footer from '../../components/v3/Footer.astro';
import WhatsAppFloat from '../../components/WhatsAppFloat.astro';
import { withBase } from '../../lib/withBase';

const title = 'Propuesta 3 · Ir y venir, hasta encontrar calma · Clínica Conecta';
const description =
  'Terapia online en español para adultos, con EMDR: ansiedad, estrés, duelo y depresión. Un espacio para procesar lo que pesa y avanzar a tu propio ritmo.';
const canonical = new URL(Astro.url.pathname, Astro.site).toString();

const whatsappMessage = encodeURIComponent('Hola, me gustaría agendar una sesión');
const whatsappHref = `https://wa.me/34644751041?text=${whatsappMessage}`;
const mailtoHref = 'mailto:riccardi.juanpsi@gmail.com?subject=Consulta%20desde%20la%20web';
const fotoCuadrada = withBase('/foto/juan-ignacio-cuadrada.webp');

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'ProfessionalService',
  name: 'Clínica Conecta',
  description,
  url: canonical,
  image: new URL(withBase('/og-image-v3.png'), Astro.site).toString(),
  telephone: '+34644751041',
  email: 'riccardi.juanpsi@gmail.com',
  areaServed: 'Comunidad hispanohablante',
  availableLanguage: 'es',
  sameAs: [
    'https://wa.me/34644751041',
    'https://www.instagram.com/juanriccardi.conecta/',
    'https://www.facebook.com/profile.php?id=61577424099534',
  ],
  founder: {
    '@type': 'Person',
    name: 'Juan Ignacio Riccardi',
    jobTitle: 'Psicólogo General Sanitario',
  },
};

const servicios = [
  {
    titulo: 'Ansiedad',
    texto:
      'Cuando la mente se adelanta y el cuerpo se queda en alerta. Trabajamos para bajar ese ritmo, con herramientas concretas.',
  },
  {
    titulo: 'Estrés',
    texto:
      'Cuando las exigencias se acumulan más rápido de lo que puedes soltarlas. Buscamos un ritmo sostenible.',
  },
  {
    titulo: 'Duelo',
    texto: 'Un proceso que no se apura. Acompaño ese ir y venir entre el recuerdo y el presente.',
  },
  {
    titulo: 'Depresión',
    texto: 'Cuando faltan las ganas de moverse. Empezamos por pasos pequeños, a tu ritmo.',
  },
];

const paraQuienEs = [
  'Sientes que la ansiedad o el estrés te superan casi todos los días.',
  'Atraviesas un duelo o una pérdida reciente.',
  'Notas que te falta motivación desde hace tiempo.',
  'Buscas un enfoque serio, con herramientas como EMDR.',
  'Prefieres hacerlo desde casa, con privacidad.',
  'Hablas español, vivas donde vivas.',
];

const pasos = [
  {
    numero: '1',
    titulo: 'Escríbeme',
    texto: 'Cuéntame qué te trae a terapia. Buscamos juntos el mejor horario para empezar.',
  },
  {
    numero: '2',
    titulo: 'Nos encontramos online',
    texto: 'Una sesión de 50-60 minutos, en un espacio privado, desde donde estés.',
  },
  {
    numero: '3',
    titulo: 'Seguimos, a tu ritmo',
    texto: 'Cada sesión ajusta el paso según cómo vayas llegando. Sin prisas.',
  },
];

const enfoques = [
  {
    titulo: 'TCC (Terapia Cognitivo-Conductual)',
    texto:
      'Parte de una idea sencilla: no son los hechos los que nos hacen sufrir, sino cómo los interpretamos y lo que hacemos a partir de ahí. Es un enfoque práctico y estructurado: identificamos juntos esos pensamientos que no te ayudan, como pensar "todo me va a salir mal" tras un solo contratiempo, y los sustituimos por formas de pensar más realistas. También vamos cambiando, poco a poco, los comportamientos que mantienen tu malestar, como evitar situaciones por miedo. Es especialmente eficaz para la ansiedad, la depresión y el estrés, y suele dar resultados visibles en relativamente poco tiempo.',
  },
  {
    titulo: 'EMDR',
    texto:
      'Es una terapia que ayuda a tu cerebro a "digerir" recuerdos o experiencias difíciles que se han quedado atascados y te siguen afectando en el presente, aunque haya pasado tiempo: ansiedad, pesadillas o reacciones desproporcionadas ante ciertas situaciones. Durante la sesión, mientras recuerdas brevemente esa experiencia, te guío a través de un tipo de estimulación (normalmente con movimientos oculares) que ayuda a tu cerebro a procesar ese recuerdo de una forma más sana. No se trata de olvidar lo ocurrido, sino de que deje de doler y de condicionar tu día a día.',
  },
];

const faqs = [
  {
    pregunta: '¿Cómo son las sesiones?',
    respuesta:
      'Sesiones individuales online, de 50 a 60 minutos, por videollamada en un espacio privado y confidencial. Están pensadas para personas adultas.',
  },
  {
    pregunta: '¿Cuánto cuesta una sesión?',
    respuesta: 'Todavía no he publicado el precio por sesión.',
    pendiente: true,
  },
  {
    pregunta: '¿Puedo hacer terapia contigo si no vivo en España?',
    respuesta:
      'Sí. Las sesiones son online y en español, así que puedes hacerlas desde cualquier país. Ten en cuenta la diferencia horaria al escribir, para encontrar un hueco que funcione para los dos.',
  },
  {
    pregunta: '¿Con qué frecuencia son las sesiones y cuánto dura el proceso?',
    respuesta:
      'Todavía no he publicado la frecuencia recomendada ni la duración media de un proceso.',
    pendiente: true,
  },
  {
    pregunta: '¿Y si no sé por dónde empezar?',
    respuesta:
      'No hace falta llegar con las palabras exactas. El primer paso también puede ser pequeño.',
  },
  {
    pregunta: '¿Qué pasa si tengo que cancelar una sesión?',
    respuesta: 'Todavía no he publicado la política de cancelación.',
    pendiente: true,
  },
];

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs
    .filter((faq) => !faq.pendiente)
    .map((faq) => ({
      '@type': 'Question',
      name: faq.pregunta,
      acceptedAnswer: { '@type': 'Answer', text: faq.respuesta },
    })),
};
---

<Base title={title} description={description} noindex image="/og-image-v3.png">
  <script
    type="application/ld+json"
    slot="head"
    is:inline
    set:html={JSON.stringify(jsonLd)}
  ></script>
  <script
    type="application/ld+json"
    slot="head"
    is:inline
    set:html={JSON.stringify(faqJsonLd)}
  ></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" slot="head" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin slot="head" />
  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@0,500;1,500;1,600&family=Work+Sans:wght@400;500;600&display=swap"
    slot="head"
  />

  <Header />

  <main class="font-v3-sans text-navy-900">
    <!-- Hero -->
    <section
      id="inicio"
      class="relative overflow-hidden bg-cream-50 px-6 pt-16 pb-20 sm:pt-24 sm:pb-28"
    >
      <div class="mx-auto max-w-3xl">
        <p class="animate-hero-in text-sm tracking-[0.2em] text-navy-900/60 uppercase">
          Juan Ignacio Riccardi · Psicólogo General Sanitario
        </p>
        <h1 class="animate-hero-in mt-5 font-v3-display text-4xl leading-tight italic sm:text-6xl">
          Ir y venir,
          <br />
          hasta encontrar calma.
        </h1>

        <div
          class="animate-hero-in relative mt-8 h-px w-36 bg-navy-100 [animation-delay:80ms]"
          aria-hidden="true"
        >
          <span class="v3-drift absolute top-1/2 left-1/2 -mt-1 -ml-1 h-2 w-2 rounded-full bg-gold-400"></span>
        </div>

        <p class="animate-hero-in mt-8 max-w-xl text-lg text-navy-900/75 [animation-delay:120ms]">
          Terapia online en español, con EMDR, para procesar lo que pesa y avanzar a tu propio
          ritmo.
        </p>
        <div class="animate-hero-in mt-8 flex flex-wrap items-center gap-5 [animation-delay:180ms]">
          <a
            href={whatsappHref}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block rounded-full bg-navy-900 px-6 py-3 font-medium text-cream-50 transition-transform duration-150 ease-[var(--ease-out)] hover:scale-[1.02] active:scale-[0.98]"
          >
            Escríbeme por WhatsApp
          </a>
          <a
            href="#sobre-mi"
            class="text-sm text-navy-600 underline underline-offset-4 hover:text-navy-900"
          >
            ¿Qué es una sesión EMDR?
          </a>
        </div>
        <p class="animate-hero-in mt-6 text-xs text-navy-900/50 [animation-delay:220ms]">
          Lo que compartas se queda aquí, protegido por el secreto profesional.
        </p>
      </div>
    </section>

    <!-- Sobre mí -->
    <section id="sobre-mi" class="bg-cream-50 px-6 py-20 sm:py-24">
      <div data-reveal class="mx-auto grid max-w-5xl items-center gap-12 md:grid-cols-[1.3fr_1fr]">
        <div>
          <p class="font-v3-display text-2xl leading-snug italic sm:text-3xl">
            "Trabajo con EMDR porque a veces no basta con hablar: hay recuerdos que el cuerpo
            necesita procesar de otra forma."
          </p>
          <div class="mt-6 space-y-4 text-navy-900/75">
            <p>
              Soy Juan Ignacio Riccardi, Psicólogo General Sanitario. Llevo casi 14 años de
              experiencia clínica, ejerciendo profesionalmente desde 2017.
            </p>
            <p>
              Combino dos enfoques con base científica: la Terapia Cognitivo-Conductual (TCC) y EMDR
              (Eye Movement Desensitization and Reprocessing), cada uno según lo que el proceso
              pida.
            </p>
            <p>
              Todo ocurre online, en español, para cualquier persona hispanohablante, sin importar
              dónde vivas.
            </p>
          </div>
        </div>

        <div class="mx-auto md:mx-0">
          <img
            src={fotoCuadrada}
            alt="Juan Ignacio Riccardi, Psicólogo General Sanitario"
            width="900"
            height="900"
            class="aspect-square w-56 rounded-full border border-navy-100 object-cover sm:w-64"
          />
        </div>
      </div>
    </section>

    <!-- Servicios -->
    <section id="servicios" class="bg-white px-6 py-20 sm:py-24">
      <div class="mx-auto max-w-5xl">
        <div data-reveal class="max-w-xl">
          <h2 class="font-v3-display text-3xl italic">Cómo puedo ayudarte</h2>
          <p class="mt-3 text-navy-900/75">
            Cada malestar tiene su propio ritmo. Encontramos el tuyo.
          </p>
        </div>

        <div
          data-reveal-group
          class="mt-10 divide-y divide-navy-100 border-t border-b border-navy-100"
        >
          {servicios.map((servicio) => (
            <div class="flex flex-col gap-2 py-6 sm:flex-row sm:items-baseline sm:gap-8">
              <h3 class="font-v3-display text-xl italic sm:w-48 sm:shrink-0">{servicio.titulo}</h3>
              <p class="text-navy-900/75 sm:flex-1">{servicio.texto}</p>
            </div>
          ))}
        </div>

        <h3 data-reveal class="mt-16 font-v3-display text-2xl italic">
          ¿Bajo qué enfoque trabajo?
        </h3>

        <div
          data-reveal-group
          class="mt-10 divide-y divide-navy-100 border-t border-b border-navy-100"
        >
          {enfoques.map((enfoque) => (
            <div class="flex flex-col gap-2 py-6 sm:flex-row sm:items-baseline sm:gap-8">
              <h4 class="font-v3-display text-xl italic sm:w-48 sm:shrink-0">{enfoque.titulo}</h4>
              <p class="text-navy-900/75 sm:flex-1">{enfoque.texto}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Cómo funciona / Para quién es -->
    <section id="como-funciona" class="bg-cream-50 px-6 py-20 sm:py-24">
      <div class="mx-auto max-w-5xl">
        <div data-reveal>
          <h2 class="font-v3-display text-3xl italic">¿Te reconoces aquí?</h2>

          <ul class="mt-6 grid gap-4 sm:grid-cols-2">
            {paraQuienEs.map((item) => (
              <li class="flex items-start gap-3 text-navy-900/75">
                <span class="mt-2.5 h-1 w-4 shrink-0 bg-gold-400" aria-hidden="true"></span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </div>

        <h3 data-reveal class="mt-16 font-v3-display text-2xl italic">
          Así es el proceso
        </h3>

        <div data-reveal-group class="mt-10 space-y-8">
          {pasos.map((paso, i) => (
            <div
              class:list={[
                'flex flex-col gap-3 sm:items-center sm:gap-6',
                i % 2 === 1 ? 'sm:flex-row-reverse sm:text-right' : 'sm:flex-row',
              ]}
            >
              <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-navy-900 font-v3-display text-lg text-cream-50 italic">
                {paso.numero}
              </div>
              <div
                class:list={[
                  'v3-connector hidden h-px flex-1 bg-navy-100 sm:block',
                  i % 2 === 1 ? 'origin-right' : 'origin-left',
                ]}
                aria-hidden="true"
              ></div>
              <div class="sm:max-w-sm">
                <h4 class="font-semibold">{paso.titulo}</h4>
                <p class="mt-1 text-sm text-navy-900/70">{paso.texto}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Preguntas frecuentes -->
    <section id="faq" class="bg-white px-6 py-20 sm:py-24">
      <div class="mx-auto max-w-3xl">
        <h2 data-reveal class="font-v3-display text-3xl italic">
          Preguntas frecuentes
        </h2>

        <div
          data-reveal-group
          class="mt-10 divide-y divide-navy-100 border-t border-b border-navy-100"
        >
          {faqs.map((faq) => (
            <details class="group py-5">
              <summary class="flex cursor-pointer list-none items-center justify-between gap-4 font-v3-display text-lg italic marker:hidden">
                {faq.pregunta}
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  class="h-4 w-4 shrink-0 text-navy-900/60 transition-transform duration-200 ease-[var(--ease-out)] group-open:rotate-45"
                  aria-hidden="true"
                >
                  <path d="M12 5v14M5 12h14" />
                </svg>
              </summary>
              <p
                class:list={[
                  'mt-3 text-sm leading-relaxed',
                  faq.pendiente ? 'font-medium text-gold-400 not-italic' : 'text-navy-900/70',
                ]}
              >
                {faq.respuesta}
              </p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- Contacto -->
    <section id="contacto" class="bg-navy-900 px-6 py-20 text-cream-50 sm:py-24">
      <div data-reveal class="mx-auto max-w-2xl text-center">
        <h2 class="font-v3-display text-3xl italic">Hablemos</h2>
        <p class="mt-3 text-navy-200">
          Cuéntame qué te gustaría trabajar. El primer paso también puede ser pequeño.
        </p>

        <div class="mt-8 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
          <a
            href={whatsappHref}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block rounded-full bg-cream-50 px-6 py-3 font-medium text-navy-900 transition-transform duration-150 ease-[var(--ease-out)] hover:scale-[1.02] active:scale-[0.98]"
          >
            Escríbeme por WhatsApp
          </a>
          <a
            href={mailtoHref}
            class="text-sm text-navy-200 underline underline-offset-4 hover:text-cream-50"
          >
            riccardi.juanpsi@gmail.com
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />

  <WhatsAppFloat href={whatsappHref} />

  <script>
    if (!matchMedia('(prefers-reduced-motion: reduce)').matches) {
      const io = new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (!entry.isIntersecting) continue;
            entry.target.setAttribute('data-visible', '');
            io.unobserve(entry.target);
          }
        },
        { rootMargin: '-80px 0px' },
      );

      document
        .querySelectorAll('[data-reveal], [data-reveal-group]')
        .forEach((el) => io.observe(el));
    }
  </script>
</Base>
